import { useMutation } from '@tanstack/react-query';import { Send, Wrench } from 'lucide-react';import ReactMarkdown from 'react-markdown';import { aiApi } from '@/api/endpoints';import { Card } from '@/components/ui/Card';import { Button } from '@/components/ui/Button';import { Input } from '@/components/ui/Input';import { useState } from 'react';
interface ChatMessage { role: 'user' | 'assistant'; content: string; references?: string[]; tools?: string[] }
export function AIChatPage() { const [input,setInput]=useState(''); const [messages,setMessages]=useState<ChatMessage[]>([]); const mutation = useMutation({ mutationFn: (message: string) => aiApi.chat(message), onSuccess: (response) => setMessages((m)=>[...m,{role:'assistant', content: response.content, references: response.references, tools: response.tool_calls}]) }); return <div className="mx-auto max-w-4xl space-y-4"><div><h1 className="text-3xl font-bold">RouteMind AI Chat</h1><p className="text-muted-foreground">Responses stream from the `/ai/chat` backend endpoint and include tool calls and references when available.</p></div><Card className="min-h-[60vh] space-y-4">{messages.map((m,i)=><div key={i} className={m.role==='user'?'text-right':'text-left'}><div className="inline-block max-w-[85%] rounded-xl bg-muted p-3 text-sm"><ReactMarkdown>{m.content}</ReactMarkdown>{Boolean(m.tools?.length) && <p className="mt-2 flex items-center gap-1 text-xs text-muted-foreground"><Wrench size={14}/>{m.tools?.join(', ')}</p>}{Boolean(m.references?.length) && <p className="mt-1 text-xs text-primary">References: {m.references?.join(', ')}</p>}</div></div>)}{mutation.isPending && <p className="text-sm text-muted-foreground">RouteMind AI is typing…</p>}</Card><form className="flex gap-2" onSubmit={(e)=>{e.preventDefault(); if(!input.trim())return; setMessages(m=>[...m,{role:'user', content: input}]); mutation.mutate(input); setInput('');}}><Input value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask about visit planning, doctors, routes, or CRM updates"/><Button disabled={mutation.isPending}><Send size={18}/></Button></form>{mutation.isError && <Button className="bg-muted text-foreground" onClick={()=>messages.at(-1)?.role==='user' && mutation.mutate(messages.at(-1)!.content)}>Retry last prompt</Button>}</div>; }
