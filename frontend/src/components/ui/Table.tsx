import { ReactNode } from 'react';
export function DataTable<T>({ rows, columns, getKey }: { rows: T[]; columns: { header: string; cell: (row: T) => ReactNode }[]; getKey: (row: T) => string }) { return <div className="overflow-auto rounded-xl border"><table className="w-full text-sm"><thead className="bg-muted"><tr>{columns.map(c=><th key={c.header} className="px-4 py-3 text-left font-semibold">{c.header}</th>)}</tr></thead><tbody>{rows.map(row=><tr key={getKey(row)} className="border-t">{columns.map(c=><td key={c.header} className="px-4 py-3">{c.cell(row)}</td>)}</tr>)}</tbody></table></div>; }
