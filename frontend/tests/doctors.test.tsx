import { render, screen } from '@testing-library/react';import { QueryClient, QueryClientProvider } from '@tanstack/react-query';import { MemoryRouter } from 'react-router';import { describe, expect, it, vi } from 'vitest';import { DoctorListPage } from '@/features/doctors/DoctorListPage';
vi.mock('@/api/doctors', () => ({ doctorsApi: { list: async () => [], bulk: async () => ({updated:0}), exportCsv: async () => new Blob() } }));
describe('DoctorListPage', () => { it('renders backend-driven doctor list controls', async () => { render(<QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}><MemoryRouter><DoctorListPage /></MemoryRouter></QueryClientProvider>); expect(await screen.findByText('Doctors')).toBeInTheDocument(); expect(screen.getByPlaceholderText(/Global search/i)).toBeInTheDocument(); }); });
