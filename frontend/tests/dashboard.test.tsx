import { render, screen } from '@testing-library/react';import { QueryClient, QueryClientProvider } from '@tanstack/react-query';import { describe, expect, it, vi } from 'vitest';import { DashboardPage } from '@/features/dashboard/DashboardPage';
vi.mock('@/api/endpoints', () => ({ dashboardApi: { visits: async () => [], doctors: async () => [], notifications: async () => [], conversations: async () => [] } }));
describe('DashboardPage', () => { it('loads sections from API queries', async () => { render(<QueryClientProvider client={new QueryClient({ defaultOptions: { queries: { retry: false } } })}><DashboardPage /></QueryClientProvider>); expect(await screen.findByText(/Field command center/i)).toBeInTheDocument(); expect(screen.getByText(/Live CRM and AI activity/i)).toBeInTheDocument(); }); });
